# Cloudflare static-asset redirects (max 3 tokens: from  to  [status]).
#
# Legacy ?id=<slug> URLs need no rule: /discovery.html is served and its
# client router resolves ?id= and #slug on its own.

# Old prototype filename → home
/celestium.html   /   301
